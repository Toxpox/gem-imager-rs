[package]
name = "gem-downloader"
version = "0.2.0"
description = "A simple async downloader for applications"
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
readme = "README.md"
keywords = ["downloader", "beagle"]
categories = ["asynchronous", "filesystem", "caching", "network-programming"]

[dependencies]
reqwest = { version = "0.13", default-features = false, features = ["stream", "http2"] }
sha2 = "0.10"
tracing = "0.1"
serde = { version = "1.0", optional = true }
serde_json = { version = "1.0", optional = true }
thiserror = "2.0"
tokio = { version = "1.52", default-features = false, features = ["fs", "sync", "rt"] }
const-hex = "1.19"
tempfile = "3.27"
gem-helper = { path = "../gem-helper", features = ["file_stream"] }
futures-util = "0.3"

[features]
default = ["rustls"]
json = ["dep:serde", "dep:serde_json"]
native-tls = ["reqwest/native-tls"]
rustls = ["reqwest/rustls"]

[dev-dependencies]
tokio = { version = "1.52", features = ["macros", "rt-multi-thread"] }
httpmock = "0.8"

[target.'cfg(not(windows))'.dependencies]
sha2 = { version = "0.10", features = ["asm"] }

[package.metadata.docs.rs]
all-features = true
